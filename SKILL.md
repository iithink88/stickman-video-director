---
name: stickman-video-director
description: 中文火柴人动画视频导演。当用户要把一段文案、笔记、文章、知识点或主题做成一分钟竖屏/横屏火柴人动画视频（线稿动态图解、励志短片、知识讲解、口播短视频）时使用。产出分镜提案 + 6 条给视频生成模型的独立提示词。触发词：火柴人视频、火柴人动画、线稿动画、stickman video、火柴人短片、把这段文案做成视频。
---

# 火柴人视频导演（中文版）

把一份素材变成一份**经确认的导演提案**，再变成 **6 条独立的约 10 秒视频生成提示词**。
保留素材原意，同时强化开头钩子、推进层次与结尾呼应。

> 本技能是「导演」：只产出提案与提示词，**不直接渲染视频**。渲染与配音见文末「本机出片路径」。
> 与 `stick-figure-knowledge-card`（静态火柴人知识卡片 / 图文）不同，本技能产出的是**动态视频**。

## 核心契约

一句话：素材 → Phase A 分镜提案（等确认）→ Phase B 六条提示词。

## 前置确认（Setup gate）

开始之前必须拿到这三样，**缺一样就用一条简短消息一次性问全，然后停下**：

1. 素材（文案 / 文章 / 知识点 / 主题）
2. 画幅：`16:9`、`9:16`（竖屏默认推荐）或 `1:1`
3. 主题：浅色（白底黑线）或深色（黑底白线）

**绝不**替用户默认画幅和主题。已经给过的选项不要重复问。
"赶时间""你随便选""按常规来"都不能免掉这一步。

## 工作流程

1. 读 `references/storyboard-template.md`，用**中文**产出 Phase A（旁白直接写中文，不写英文再翻译）。
2. 产出导演提案后**停下来，请求用户明确确认**。
3. 若用户改了画幅、主题、旁白、分场结构或整体风格 → 重做 Phase A 并再次请求确认。
4. 只有在当前 Phase A 被批准之后，才读 `references/omni-flash-prompt-contract.md` 产出 Phase B。
5. 仅当需要一个端到端样例来消除歧义时，才读 `references/examples.md`。

素材本身被认可、时间紧、或批准过旧版本 —— 都**不等于**批准当前这份 Phase A。

## 输出规则

- **中文旁白总量 240–300 字**，分摊到 6 条片子（约 55–65 秒普通话语音，按 4.5 字/秒估算）。
- 每条片子三个时间节拍（`0–3s` / `3–7s` / `7–10s`），至少 4 个与内容相关的视觉手法，每 2–3 秒有一次可见变化。
- 保持人物比例、线宽、主题、旁白音色一致。
- 全片最多三种饱和强调色，只用"鲜红""电光蓝""暖金"这类**日常描述词**命名。
- **禁止**在给模型的提示词里出现十六进制、RGB、HSL、潘通等技术色值——模型会把它当文字画进画面。
- 浅色主题：要求**平整、均匀、数字纯白**画布，禁止灰/米白偏色、纹理、渐变、阴影、光照、辉光、雾气与三维背景纵深。不要用色号描述白色。
- 每条提示词自包含，重复所有关键锁定项。
- 旁白只作为**音频**存在：原样引用台词，禁止改写、重复、上字幕或在画面里转写文字。
- 默认画面**不出现任何文字、字母、数字、界面文案、技术标注**；气泡、卡片、进度条、时钟、通知一律做成纯图标。可选的 2–5 字叠加字幕放在**后期说明**里，绝不写进生成提示词。
- 每条片子的结尾必须能接上下一条的开头。
- 不编造无依据的事实、数据、引语或产品宣称。

**交付形态**：Phase B 的 6 条提示词较长，**写成 `.md` 文件交付**（内含全局连贯块、6 条独立提示词、
缝合指南、音画连贯说明、自检清单、后期字幕表），聊天里只回一份摘要表（每条的画面要点 + 台词）。

## 修改规则（Recompose，不是改名）

- `16:9`：动作铺在左中右，横向运镜 + 留白。
- `9:16`：用纵深、堆叠运动、纵向揭示、避开竖屏界面遮挡区。
- `1:1`：紧凑居中构图，运动路径更短。

换主题 = 背景与线条黑白反转，强调色的语义与对比关系保持不变。**任何全局改动都会作废此前的批准。**

## 本机出片路径（可选）

Phase B 拿到 6 条提示词后，按本机环境渲染：

| 步骤 | 做法 |
|---|---|
| 画面 | 优先用 **VideoGen** 工具（内置）；用户若配了第三方（如 apiz 的 `minimax/h3`、可灵、Veo），按其指定调用。逐条生成 6 段约 10 秒素材 |
| 配音 | `edge-tts --voice zh-CN-XiaoxiaoNeural`（默认）或 apiz `minimax/t2a` 的 `female-yujie`；用 Phase A 定稿的中文旁白，整段一次生成 |
| 合成 | ffmpeg：6 段画面按缝合指南顺序拼接 → 铺旁白 + BGM（人声优先）→ 需要时画面与音频**一起**加速（如 1.3×），否则音画会错开 |
| 字幕 | 可选：把 Phase A 的后期叠加词用 ffmpeg drawtext 烧进去 |

**配音与画面一起加速**是硬要求，只加速画面会导致口型与声音对不上。

## 最终检查

按所加载 reference 里的检查清单逐条核对，任何一条不过就先修再回复用户。
